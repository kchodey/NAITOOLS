import { NuidValidatorDirective } from './nuid-validator.directive';

describe('NuidValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NuidValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
