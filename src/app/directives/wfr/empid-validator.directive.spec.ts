import { EmpidValidatorDirective } from './empid-validator.directive';

describe('EmpidValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpidValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
